public class Velocity {
    final static int distance = 100;
    final static int time = 10;

    public static void main(String[] args) {
        int velocity = distance / time;
        System.out.println("If the distance travelled is " + distance + " meters, covered in " + time + " seconds, then the velocity is " + velocity + " m/s");
    }
}
